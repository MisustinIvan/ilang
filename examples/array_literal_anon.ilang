extrn unit printf(string format, ...)

unit print_arr([n]int arr) {
    if n >= 3 {
        printf("%d %d %d\n", arr[0], arr[1], arr[2]);
    }
}

int main() {
    print_arr([100, 200, 300]);
    0
}
